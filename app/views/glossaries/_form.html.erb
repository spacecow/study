<%= simple_form_for @glossary do |f| %>
  <%= f.input :japanese %>
  <%= f.input :sentence_tokens, :label => pl(:sentence,1), input_html:{data:{pre:@glossary.sentences, url:sentences_path(:format=>:json)}} %> <%= f.submit %> <% end %>
