<% present lookup do |presenter| %>

<li class="sentence">
  <div class="content">
    <%= presenter.japanese %>
    <%= presenter.english %>
    <%= presenter.meaning if meaning %>
    <%= presenter.associations %>
  </div>
  <%= presenter.glossaries if glossaries %>
  <div class="clear"></div>
</li>

<% end %>
